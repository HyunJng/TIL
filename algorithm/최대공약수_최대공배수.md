# 최대공약수, 최대공배수
## 유클리드 호제법 이용
* 최대공약수: gcd(a, b) = gcd(b, a mod b)
* 최대공배수: lcm(a, b) = a * b / gcd(a, b)
* 예시 코드(Java):
```java
    public int gcd(int a, int b){
        if(b == 0) return a;
        return gcd(b, a % b);
    }
```